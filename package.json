{
  "name": "idbx",
  "version": "1.0.3",
  "description": "No-bullshit indexedDB wrapper functions with promises, iterators and shortcuts.",
  "types": "dest/main.d.ts",
  "module": "dest/main.js",
  "type": "module",
  "scripts": {
    "ts-types": " tsc --emitDeclarationOnly --outDir dest",
    "replaceDts": "find dest -type f -name \"*.d.ts\" -exec sh -c 'sed \"s/\\.ts/\\.d.ts/g\" \"$0\" > \"$0.tmp\" && mv \"$0.tmp\" \"$0\"' {} \\;",
    "build": "rimraf dest && node ./esbuild.js && npm run ts-types && npm run replaceDts"
  },
  "keywords": [],
  "author": "Mario Stöcklein <mstoecklein@xp.live>",
  "license": "BSD-2-Clause",
  "devDependencies": {
    "esbuild": "^0.18.8",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.3"
  }
}
